services:
  server:
    build: .
    environment:
      VITE_BASE_URL: /pcv-beta
    ports:
      - 13029
    restart: always
    labels:
      - webproxy.path=pcv-beta
      - webproxy.websockets=true